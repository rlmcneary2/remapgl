---
menu: API
name: Navigation
route: /navigation
---

import { Props } from "docz";
import Navigation from "./navigation";

# Navigation

The `<Navigation/>` component is a child of [Map](/map), it creates the navigation control in the map that contains zoom buttons and a compass.

## Props

<Props of={Navigation} />
