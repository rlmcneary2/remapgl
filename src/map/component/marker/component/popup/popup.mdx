---
menu: API
name: MarkerPopup
route: /markerpopup
---

import { Props } from "docz";
import MarkerPopup from "./popup";

# MarkerPopup
The `<MarkerPopup/>` component is the value of the `popup` prop of [Marker](/marker), it provides information when the user manipulates a control.

## Props
Props that can be passed to every `<Popup/>` component.

<Props of={MarkerPopup} />
