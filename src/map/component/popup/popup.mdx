---
menu: Components
name: Popup
route: /src/popup/popup.mdx
---

import { Props } from "docz";
import Popup from "./popup";

# Popup
The `<Popup/>` component is a child of [Map](../map) or returned by the `popup` prop of [Marker](/marker), it provides information when the user manipulates a control.

## Props
Props that can be passed to every `<Popup/>` component.

<Props of={Popup} />
