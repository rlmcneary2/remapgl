---
menu: API
name: Scale
route: /scale
---

import { Props } from "docz";
import Scale from "./scale";

# Geolocate

The `<Scale/>` component is a child of [Map](/map), it creates the scale control
in the map that displays the ratio of a distance on the map to the corresponding
distance on the ground.

## Props

<Props of={Geolocate} />
