---
name: Popup Component
route: /popup
---

import { Props } from "docz";
import Popup from "./popup";

# Popup
The `<Popup/>` component is a child of [Map](../map) or returned by the `popup` prop of [Marker](../marker/marker.md), it provides information when the user manipulates a control.

## PopupProps
Props that can be passed to every `<Popup/>` component.

<Props of={Popup} />
