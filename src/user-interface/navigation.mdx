---
menu: Components
name: Navigation
route: /src/user-interface/navigation.mdx
---

import { Props } from "docz";
import Navigation from "./navigation";

# Navigation

The `<Navigation/>` component is a child of [Map](./src/map/map.mdx), it creates the navigation control in the map that contains zoom buttons and a compass.

## Props

<Props of={Navigation} />
